# 更新日志（CHANGELOG）

MyDesk 个人工作台（PersonalWorkstation）。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.2.0] - 2026-09-11

体验打磨：无任务专注模式、习惯卡片改版、侧栏图标分色与一处统计口径修复。

### 新增

- **无任务专注模式**：心流专注页新增「无任务专注」区——🧘冥想 / 📖读书 / 🏃运动 / ✍️写作 / 🎧听播客 / 🚶散步六张模式卡，点击即以该模式开始计时，不挂任务也能专注；模式对应隐藏任务（`focus_mode:` 前缀）自动创建且不在看板显示，工时统计与计时条、快速面板均以模式名展示。
- **侧栏图标分色**：九个模块图标各有专属色（青/蓝/紫/琥珀/红/绿/金/粉/天蓝），随模块固定、不随主题色变化，增加辨识度与灵动感。

### 变更

- **习惯打卡卡片化改版**：习惯列表由 checkbox 行改为**双列卡片网格**——卡片含名称、频率、🔥连续天数、强度%，右侧圆形 ✓ 打卡按钮（打卡时 pop 弹跳）；选中卡片查看详情。
- **统计块替代热力图**：详情区 52 周热力图（小黑框）替换为六格大数字统计块——本周/本月/本年打卡次数 + 连续天数 + 最长连续 + 强度%；命中里程碑时统计区显示 🎉 提示。
- **常用速建常显**：速建 chips 行不再因建了同名习惯而整行消失；已存在的 chip 置灰带 ✓，六个全部建完才隐藏。

### 修复

- **速览「今日任务」口径**：卡片「今日待办」此前只统计截止时间恰好是今天的任务，无截止/未来截止的待办被漏计导致与看板不一致；现改为统计全部未完成任务，与顶栏口径一致。
- **速览/顶栏统计排除专注模式隐藏任务**：「今日待办」与顶栏「未完成任务」此前直接统计全量任务，把「无任务专注」创建的 `focus_mode:` 隐藏任务（看板上不可见）也计入，导致看板为空时仍显示 3 个待办；现统计口径与看板一致，隐藏任务不计入待办/进行中/今日完成/逾期/即将到期/周报与顶栏统计（今日工时仍按模式名正常展示）。
- **侧栏图标对齐**：不同 Unicode 字符宽度不一导致模块名左右参差；图标格固定宽度居中后全部对齐。

## [1.1.0] - 2026-09-11

年轻化升级：模块四字命名、两个新模块（随手记账 / 轻盈计划）、习惯打卡仪式感。

### 新增

- **随手记账**（新模块）：支出一笔、收入一笔——金额（整数分存储）、分类 chips（餐饮🍜/交通🚌/购物🛍/娱乐🎮/居住🏠/医疗💊，收入：工资/兼职/红包/其他）、备注与日期；月视图 ‹ › 切月，月度收入/支出/结余汇总，**月预算进度条**（超支变红，预算存 settings 表 `ledgerBudget` 键），**支出分类占比条形图**，按日分组账目流水（悬停显示编辑/删除）。
- **轻盈计划**（新模块）：**体重打卡**（每天一条，同日重记覆盖，目标虚线 + SVG 自绘近 90 天趋势折线）+ BMI（配身高设置）+ 目标体重与已减进度；**锻炼记录**（跑步🏃/力量🏋/游泳🏊/骑行🚴/球类🏸/瑜伽🧘/其他 × 时长 × 备注），本周（周一起算）与本月次数/分钟统计。身高/目标分别存 settings 表 `fitnessHeight` / `fitnessGoalWeight` 键。
- **打卡仪式感**：勾选瞬间 checkbox pop 弹跳 + 🔥 连续天数 bump；连续 7/21/66/100/365/500/1000 天命中里程碑弹「🎉 连续 X 天」toast；当日全部习惯打卡完成触发更大粒子彩蛋；庆祝粒子层（`Celebration.tsx`）在打卡行位置喷发彩纸，`prefers-reduced-motion` 下不喷发。
- **今日速览新增两张卡片**：「本月支出」（支出/结余直达账本）、「轻盈计划」（最新体重 + 本周锻炼次数）。

### 变更

- **模块四字命名**（盖特当下年轻人）：概览→**今日速览**、专注→**心流专注**、日历→**时光月历**、习惯→**习惯打卡**、笔记→**灵感速记**；描述文案同步年轻化。模块 id 不变，老数据与设置零迁移。
- **数据库 v10**：新增 `ledger_entries` / `weight_logs` / `workout_logs` 三表（自动迁移，老库升级保数据）；JSON 备份导入导出覆盖三张新表（v9 及更早备份仍可导入，新表视为空）；新增 12 个 Tauri 命令（ledger_list/create/update/delete、weight_list/upsert/delete、workout_list/create/update/delete）。
- 页面文案联动：概览「最近速记 / 今日打卡」、小组件「最近速记」、快速面板「搜索任务/速记/入口」、日历面板「打开/创建当日速记」。

## [1.0.2] - 2026-09-11

UI 轻量视觉刷新：设计 token 体系、克制微动效、四主题色选项。

### 新增

- **主题色选项**：设置 → 外观新增主题色选择器——青碧（新默认）/ 靛蓝 / 珊瑚 / 黛青四色即时切换并持久化（settings 表 `accent` 键）；品牌渐变字、侧栏选中态等全部跟随变化。
- **三窗口主题同步**：主窗口通过 `theme://changed` 事件广播主题与主题色，快速面板与桌面小组件启动时读取设置并实时跟随（`useThemeSync` hook）。
- **克制微动效**：按钮/列表/卡片过渡基线（120-180ms）、卡片 hover 浮起加阴影、输入框聚焦淡色 ring、弹层出场缩放动画、页面切换淡入；`prefers-reduced-motion` 下全部关闭。

### 打磨

- **设计 token 体系**：主题色包（accent/hover/soft/contrast/brand-grad × 浅深两态）、圆角三档（6/8/12px，替换原 4-14px 混用）、阴影两档、遮罩色、动效时长，集中定义于 `index.css` 顶部；硬编码阴影/渐变/圆角全部归档。
- 键盘焦点可见性（`:focus-visible` outline）、webkit 滚动条细化（窄条 + 主题色）。
- 深色模式悬浮层提亮一档（`--panel-pop`），弹层边框改用 `--border-strong`。
- 优先级四色在 CSS 与 `TaskCard.tsx` 两处加互指注释，防止后续改色失同步。
- `index.html` 标题与语言由脚手架默认改为 MyDesk / zh-CN。

### 修复

- **快速面板与桌面小组件不跟随主题**：两窗口此前未调用 `applyTheme`，深浅色设置对它们无效；现统一接入 `useThemeSync`。
- **`.month-grid` 双重定义冲突**：日历页实线网格版与日期选择弹层间隙版属性互相覆盖；拆为 `.month-grid-page` / `.month-grid-popup` 两个类（`MonthGrid` 新增 `variant` prop）。
- `.settings-row small` 重复定义合并。

## [1.0.1] - 2026-09-10

三项用户反馈修复。

### 修复

- **桌面小组件白板**：WidgetWindow 此前在 React 管理的 root 容器里手动 `appendChild` 渲染（组件本体 `return null`），React 挂载/重渲染时清空容器导致内容被抹掉。改为纯 React 声明式渲染（useState + useEffect 轮询）。
- **专注无入口**：番茄钟唯一入口藏在「计时中才显示」的计时条里、任务计时器入口过深。新增**专注模块页**（侧栏 ⏱，默认启用）：任务选择、开始/停止计时、番茄钟档位与启动、今日工时按任务分布；概览「今日专注」卡片点击改跳本页。
- **深色主题下习惯提醒时间选择器被遮挡**：全局补 `color-scheme: light/dark` 声明，原生 `<input type=time>` 时钟图标与下拉面板跟随主题渲染。

## [1.0.0] - 2026-09-10

桌面版：存在感延伸与工程化补课，v1.0 正式发布。

### 新增

- **桌面常驻小组件窗口**（默认关）：桌面右下角置顶半透明副窗口，显示今日任务（可勾选完成）、下一次提醒与最近笔记，点击条目唤起主窗跳转；30 秒自动刷新；设置页开关。
- **设置页增强**：番茄钟专注/休息分钟数自定义（1-120/1-60）；桌面小组件开关；「关于」卡片。
- **软件内版本标识**：主窗侧栏底部与设置页「关于」显示版本号（构建期注入）。
- **工程化**：GitHub Actions CI（windows-latest：vitest + build + cargo test）；CHANGELOG；`npm run dist` 打包脚本自动产出 `MyDesk-v<version>-setup.exe` 命名。

### 打磨

- 任务看板页顶部搜索框（标题+描述过滤，纯函数 TDD）。
- 笔记编辑器状态栏显示字数（去空白计数）。
- 修复快速面板窗口缺失 CSS 引入导致打包后无样式的缺陷（v0.2 引入 quick.html 时遗漏）。

### 放弃（v1.0 范围内）

- 通知点击唤起主窗：tauri-plugin-notification 2.x 无通知点击回调 API，托盘左键已有同等能力。
- 日历周视图：低优先级。

## [0.9.0] - 2026-09-10

检索版：全局搜索与笔记连接。

- 全局搜索：SQLite FTS5 虚拟表索引任务/笔记/快捷入口，触发器实时同步；自定义 `cjk_space` 函数实现中文按字匹配；快速面板一框搜全部、分类展示、回车直达。
- 笔记 `[[双向链接]]`：输入 `[[` 弹出标题自动补全；链接区块点击跳转（目标不存在可一键创建）；「引用了它」反链面板。
- 备份格式 v9（FTS 索引不导出，导入后重建）。

## [0.8.0] - 2026-09-10

专注版：时间维度闭环。

- 任务计时器：看板卡片 ▶ 与抽屉「开始专注」，同一时间仅一个计时，time_entries 表落库。
- 番茄钟：25/5 等档位，可绑定计时任务；到点自动停表并弹系统通知，休息结束提醒；托盘 tooltip 显示「正在专注：X (mm:ss)」与番茄倒计时。
- 概览「今日专注」卡片：总时长与按任务分布 Top3。
- 备份格式 v8。

## [0.7.0] - 2026-09-10

习惯版：新模块 + 任务闭环。

- 习惯追踪模块：打卡、52 周热力图、连续天数；强度评分移植 Loop Habit Tracker 官方 EWMA 算法（`0.5^(√f/13)`，非每日习惯频率加倍平滑）；习惯提醒（到点且当日未打卡才通知）。
- 重复任务：每天/每周/每月三档，完成时自动生成下一单；卡片 ↻ 徽标。
- 概览「今日习惯」卡片。
- 数据库迁移 v7（habits/habit_logs + tasks.repeat）；备份格式 v7。

## [0.6.0] - 2026-09-10

焊接版：低成本高联动。

- 每日笔记：日历一键打开/创建当日笔记（「9月10日」式标题）。
- 快速面板语法增强：「每天 9:00」「每周五 18:00」「每月15号」准点提醒与「#标签」描述前缀。
- 快速面板入口模式：空格列出前九个入口序号直达，`/关键词` 过滤回车即开。
- 概览「本周回顾」卡片：完成/新建/逾期与上周环比。
- 修复模块注册表与设置存储的循环依赖（拆分 `modules/meta.ts`）。

## [0.5.0] - 2026-09-09

- 多看板：boards 表，看板切换器（新建/重命名/删除，默认看板拒删），任务归属看板，抽屉改归属。
- 备份格式 v6（含看板，导入后保证默认看板存在）。

## [0.4.0] - 2026-09-09

- 概览模块：今日任务统计、即将到期、最近笔记、常用入口四卡片聚合首页。

## [0.3.0] - 2026-09-08

- 快捷入口模块：网址/文件/命令一键启动，网格卡片拖拽排序，关键字过滤。

## [0.2.0] - 2026-09-08

- 笔记模块：纯文本双栏编辑、搜索、置顶、1 秒防抖自动保存。

## [0.1.0] - 2026-09-07

- 首个可用版本：任务看板（三列拖拽）、日历日程、到期系统通知、快速面板（Alt+Space，自然语言建任务）、系统托盘、模块化开关、SQLite 本地存储与 JSON 备份、三态主题。
